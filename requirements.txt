Flask==2.3.0
pymongo==4.3.3
python-dotenv==1.0.0
werkzeug==2.3.0
pytesseract==0.3.10
requests==2.31.0
# Pillow==8.4.0  # Commented out - requires Visual C++ build tools on Windows
# Install separately later if needed: pip install Pillow --only-binary :all: